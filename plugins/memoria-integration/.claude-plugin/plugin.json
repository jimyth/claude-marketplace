{
  "name": "memoria-integration",
  "version": "1.0.0",
  "description": "自主记忆系统 — 跨会话、跨项目的持久化 LLM 记忆，完全自主演化，可追溯变更历史",
  "author": {
    "name": "Jimyth"
  },
  "license": "MIT",
  "keywords": ["memory", "persistence", "context", "autonomous", "mcp"],
  "userConfig": {
    "mcp_server_url": {
      "type": "string",
      "title": "Memoria MCP Server URL",
      "description": "Memoria 服务的 SSE 地址，例如 http://localhost:8081/sse 或 http://192.168.1.100:8081/sse?project_id=my-project",
      "required": true
    }
  },
  "mcpServers": {
    "memoria": {
      "type": "sse",
      "url": "${user_config.mcp_server_url}"
    }
  }
}
